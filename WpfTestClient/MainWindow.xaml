<Window x:Class="WpfTestClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfTestClient"
        Title="FrogTail gRPC Test Client"
        Height="760" Width="900"
        MinHeight="680" MinWidth="800">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        <local:InverseBoolConverter x:Key="InverseBoolConverter" />
    </Window.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"    />
        </Grid.RowDefinitions>

        <!-- 1. 서버 설정 -->
        <GroupBox Grid.Row="0" Header="서버 설정" Margin="0,0,0,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="서버 주소:" />
                <TextBox Grid.Column="1"
                         Text="{Binding ServerAddress, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />
                <Button Grid.Column="2"
                        Content="채널 재구성"
                        Command="{Binding RebuildChannelCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />
            </Grid>
        </GroupBox>

        <!-- 2. 세션 정보 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,2,0,4">
            <Label Content="현재 세션: " FontWeight="Bold" />
            <Label Content="{Binding SessionInfo}" Foreground="DarkBlue" />
        </StackPanel>

        <Separator Grid.Row="2" />

        <!-- 3. 로그인 패널 -->
        <GroupBox Grid.Row="3" Header="로그인" Margin="0,4,0,4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="DeviceId:" />
                <TextBox Grid.Row="0" Grid.Column="1"
                         Text="{Binding DeviceId, UpdateSourceTrigger=PropertyChanged}" />
                <Label Grid.Row="0" Grid.Column="2" Content="NickName:" />
                <TextBox Grid.Row="0" Grid.Column="3"
                         Text="{Binding NickName, UpdateSourceTrigger=PropertyChanged}" />

                <Label Grid.Row="1" Grid.Column="0" Content="LoginType:" Margin="0,4,4,0" />
                <ComboBox Grid.Row="1" Grid.Column="1"
                          x:Name="LoginTypeCombo"
                          SelectedItem="{Binding LoginType}"
                          Margin="0,4,4,0" />

                <Label Grid.Row="1" Grid.Column="2" Content="AccessToken:" Margin="0,4,4,0" />
                <TextBox Grid.Row="1" Grid.Column="3"
                         Text="{Binding AccessToken, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Guest 신규: 비워두세요 (서버에서 GuestToken 발급 후 자동 채움). Guest 재로그인: 신규 로그인 후 자동 채워진 값. Firebase: JWT AccessToken."
                         Margin="0,4,0,0" />

                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4"
                            Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,4,0,0">
                    <Button Content="로그인"
                            Command="{Binding LoginCommand}"
                            ToolTip="LoginType에 따라 로그인합니다." />
                </StackPanel>
            </Grid>
        </GroupBox>

        <Separator Grid.Row="4" />

        <!-- 4. API 호출 -->
        <GroupBox Grid.Row="5" Header="API 호출 (인증 필요)" Margin="0,4,0,4">
            <StackPanel Orientation="Horizontal">
                <Button Content="ShopList 조회"
                        Command="{Binding GetShopListCommand}"
                        ToolTip="로그인 후 사용 가능. x-userid + authorization 헤더 자동 첨부." />
                <Button Content="로그 초기화"
                        Command="{Binding ClearLogCommand}"
                        Background="#FFFDD0" />
            </StackPanel>
        </GroupBox>

        <Separator Grid.Row="6" />

        <!-- 5. 시나리오 패널 -->
        <GroupBox Grid.Row="7" Header="패킷 시나리오" Margin="0,4,0,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="구성:" FontWeight="Bold" />
                <TextBox Grid.Column="1"
                         Text="{Binding ScenarioSummary, Mode=OneWay}"
                         IsReadOnly="True"
                         Background="#F5F5F5"
                         ToolTip="시나리오 구성 버튼으로 패킷 순서를 설정하세요." />
                <Button Grid.Column="2"
                        Content="시나리오 구성"
                        Command="{Binding OpenPacketListCommand}"
                        Margin="4,0,0,0" />
                <Button Grid.Column="3"
                        Content="시나리오 실행"
                        Command="{Binding RunScenarioCommand}"
                        Margin="4,0,0,0"
                        Background="#E8F5E9" />
            </Grid>
        </GroupBox>

        <Separator Grid.Row="8" />

        <!-- 6. 로그 뷰어 -->
        <Grid Grid.Row="9">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="4" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Request Log">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox Text="{Binding RequestLog, Mode=OneWay}"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             FontFamily="Consolas"
                             FontSize="11"
                             Background="#F0F0F0"
                             BorderThickness="0"
                             AcceptsReturn="True"
                             TextChanged="RequestTextBox_TextChanged" />
                </ScrollViewer>
            </GroupBox>

            <GridSplitter Grid.Column="1" Width="4"
                          HorizontalAlignment="Center" VerticalAlignment="Stretch" />

            <GroupBox Grid.Column="2" Header="Response Log">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox Text="{Binding ResponseLog, Mode=OneWay}"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             FontFamily="Consolas"
                             FontSize="11"
                             Background="#F0FFF0"
                             BorderThickness="0"
                             AcceptsReturn="True"
                             TextChanged="ResponseTextBox_TextChanged" />
                </ScrollViewer>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
